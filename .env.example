# Email for the OpenAlex polite pool (required — the ETL refuses to start without it).
OPENALEX_MAILTO=you@example.com

# Database URL. SQLite file by default; PostgreSQL in production.
SFR_DB_URL=sqlite:///data/sfr.db
